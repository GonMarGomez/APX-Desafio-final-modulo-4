function main() {
  const header = document.querySelector(".contacto__header");
  const form = document.querySelector(".formulario-de-contacto");
  const footer = document.querySelector(".footer");
  footerComponent(footer);
  headerComponent(header);
  formComponent(form);
}
main();
